package schelper.core

import scala.swing.Color

object SchelperConstants {
  val ApplicationName = "Schelper"
  val MainWindowPosition = "MainWindowPosition"
  val SourcePaths = "SourcePaths"
  val SourceNames = "SourceNames"
  val LibraryPaths = "LibraryPaths"
  val LibraryNames = "LibraryNames"
  val ScalacPath = "ScalacPath"
  val BuildPath = "BuildPath"
  val JavaPath = "JavaPath"
  val AppClass = "AppClass"

  val BackgroundColour = new Color (255, 255, 255)
  val NavigateColour = new Color (200, 200, 255)
  val SourceColour = new Color (255, 255, 200)
  val ActionColour = new Color (255, 200, 255)
  val SectionColour = new Color (200, 255, 255)
}
